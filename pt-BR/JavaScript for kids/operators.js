 /**
  * Text in the book ;D
  */
class Operators {
   
    static sample() {
        const oldSolarSystem = 9;
        const newSolarSystem = 8;
        const plutaoResult = !true ? "é" : "não é";

        console.log(
            `O sistema solar tinha ${oldSolarSystem} planetas mas agora tem `
            + `${newSolarSystem} e Plutão ${plutaoResult} um deles.`
        );
    }
}

Operators.sample();
